{
  "name": "mtg-quick-reference",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm run rules && tsc --noEmit && vite build && node scripts/build-sw.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "data": "python3 scripts/build_data.py",
    "test": "node --experimental-strip-types --test tests/*.test.ts && python3 -m unittest discover -s tests -p 'test_*.py'",
    "bench": "node --experimental-strip-types scripts/benchmark.ts",
    "rules": "python3 scripts/parse-production-rules.py && node --experimental-strip-types scripts/build-rules.ts",
    "test:research": "python3 -m unittest discover -s research/omnisearch/tests && node research/omnisearch/prepare.mjs && node --test research/omnisearch/tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
